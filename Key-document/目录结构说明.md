# 项目目录结构说明

> 本文档说明仓库中每个目录和关键文件的用途，便于接手者快速定位代码。

---

## 根目录

```
d:\algorithm-optimization\
├── README.md                — 项目总览、快速启动、文档索引
├── paper_text.txt           — （空文件，预留论文内容）
├── .gitignore               — Git 忽略规则
├── Drone_Tracking_RA6M5/    — RA6M5 主控固件（核心工程）
├── Drone_Tracking_WebCloud/  — 云端看板（FastAPI + SQLite）
├── ESP32-Competition_Deliverables/ — ESP32 网关固件
├── Key-document/            — 核心设计文档
└── openmv-code/             — OpenMV 磁盘部署文件（模型 + 标签 + 脚本）
```

---

## 1. `Drone_Tracking_RA6M5/` — RA6M5 主控固件

这是整个系统的**控制核心**，基于瑞萨 e² studio + FSP 框架开发。

```
Drone_Tracking_RA6M5/
├── configuration.xml          — FSP 图形化配置文件（UART、引脚、时钟）
├── Drone_Tracking_RA6M5 Debug_Flat.launch  — 调试启动配置
├── ra_cfg.txt                — RA 配置摘要
│
├── src/                       — ★ 应用层源码（开发者最常改的地方）
│   ├── hal_entry.c            — 主事件循环：UART 中断、LED 状态、遥测发送、舵机初始化
│   ├── hal_warmstart.c        — 热启动处理
│   ├── openmv_drv.c/h         — OpenMV 视觉帧接收驱动（8 字节协议解析，双缓冲）
│   ├── control/               — 控制算法
│   │   ├── pid_controller.c/h      — 增量式 PID（IIR 滤波、死区、积分分离）
│   │   └── tracking_controller.c/h — 四态追踪状态机、雷达 Kalman、多模融合、增益调度
│   └── drivers/               — 硬件驱动
│       ├── ld2450_driver.c/h       — LD2450 毫米波雷达帧解析（30 字节协议 + IIR 平滑）
│       └── st3215_driver.c/h       — ST-3215 舵机通信驱动（飞特协议、半双工、SYNC_WRITE）
│
├── ra_gen/                    — FSP 自动生成代码（Generate Project Content 产出）
│   ├── main.c                 — 入口：调用 hal_entry()
│   ├── hal_data.c/h           — UART 实例配置（g_uart2/3/5/9 的波特率、回调、引脚）
│   ├── common_data.c/h        — 通用外设初始化
│   ├── vector_data.c/h        — 中断向量表
│   ├── pin_data.c             — 引脚复用配置
│   └── bsp_clock_cfg.h        — 时钟配置
│
├── ra_cfg/                    — FSP 模块配置头文件
│   └── fsp_cfg/
│       ├── bsp/               — BSP 配置（芯片型号、引脚、OFS）
│       ├── r_ioport_cfg.h     — IO 端口配置
│       ├── r_iwdt_cfg.h       — 看门狗配置
│       └── r_sci_uart_cfg.h   — SCI UART 配置
│
├── ra/                        — FSP 框架源码（一般不需要修改）
│   ├── arm/CMSIS_6/           — ARM CMSIS 核心头文件
│   └── fsp/
│       ├── inc/               — FSP API 头文件（r_uart_api.h 等）
│       └── src/
│           ├── bsp/           — BSP 实现（启动、时钟、中断、IO）
│           ├── r_sci_uart/    — SCI UART 驱动实现
│           ├── r_ioport/      — IO 端口驱动
│           └── r_iwdt/        — 看门狗驱动
│
├── openmv_scripts/            — OpenMV 机载脚本（部署到 OpenMV 的正本）
│   └── main_tracking.py       — FOMO 目标检测 + MA + Kalman + 8 字节帧发送
│
├── script/
│   └── fsp.ld                 — FSP 链接脚本
│
├── Debug/                     — 编译输出（可再生，不要手动编辑）
│   ├── *.elf.in, *.map, *.srec, *.rpd  — 固件二进制
│   ├── compile_commands.json   — 编译数据库（clangd 代码补全用）
│   ├── fsp_gen.ld, memory_regions.ld  — 生成的链接脚本
│   ├── makefile               — 构建系统
│   └── ra_gen/, src/, ra/     — 各模块的 .o/.d 编译中间文件
│
└── .settings/                 — e² studio IDE 首选项（不需要手动编辑）
```

### 开发者重点关注

| 你想做什么 | 去哪里 |
|-----------|--------|
| 改追踪算法/状态机 | `src/control/tracking_controller.c` |
| 调 PID 参数 | `src/control/tracking_controller.c` 里的默认初始化，或 `pid_controller.c` |
| 改雷达解析逻辑 | `src/drivers/ld2450_driver.c` |
| 改舵机通信/限位 | `src/drivers/st3215_driver.c` |
| 改视觉帧协议 | `src/openmv_drv.c` + `openmv_scripts/main_tracking.py` 两端同步 |
| 改遥测 JSON 格式 | `src/hal_entry.c` 里的 sprintf |
| 改 UART 引脚/波特率 | `configuration.xml` → 重新 Generate → 查看 `ra_gen/hal_data.c` |

---

## 2. `Drone_Tracking_WebCloud/` — 云端监控看板

```
Drone_Tracking_WebCloud/
├── main.py              — FastAPI 后端（JWT认证、RBAC、WebSocket、REST API）
├── requirements.txt     — Python 依赖（fastapi, sqlalchemy, uvicorn 等）
├── .env.example         — 环境变量模板（JWT密钥、数据库URL、管理员账户）
├── login.html           — 登录页
├── dashboard.html       — 实时数据看板（WebSocket 推送）
├── admin.html           — 管理后台（用户/位置/摄像头管理）
├── drone_tracking.db    — SQLite 运行时数据库（首次启动自动创建）
├── __pycache__/         — Python 编译缓存
└── .venv/               — Python 虚拟环境
```

关键 API 端点见 → `Key-document/接口协议文档.md`

---

## 3. `ESP32-Competition_Deliverables/` — ESP32 遥测网关

```
ESP32-Competition_Deliverables/
├── esp32_async_gateway/
│   └── esp32_async_gateway.ino  — ESP32 Arduino 固件：
│                                   接收 RA6M5 遥测 JSON (UART2)
│                                   WiFi HTTP POST 上报到云端
│                                   FreeRTOS 异步队列 + 错误退避
│
└── ra6m5_tracking_core_update.c — RA6M5 追踪核心的更新参考代码
                                    （与 src/control/ 下的实现对照查看）
```

---

## 4. `Key-document/` — 核心设计文档

```
Key-document/
├── core-code.md              — 算法核心：FOMO 后处理、IIR/Kalman 滤波、增量 PID、四态状态机
├── PID调制.md                — PID 理论 + 本项目调参实战方法论
├── 系统理性运行期望状态.md     — 系统五层运行目标定义
├── 硬件接线连接.md            — 四路 UART 物理接线（引脚、波特率、注意事项）
├── 接口协议文档.md            — 四路 UART 帧格式逐字节定义 + 状态机转移条件
├── 参数速查表.md              — 所有关键常量集中索引（附源码行号）
├── 已知问题与陷阱.md          — 经代码审计确认的设计缺陷和调试经验
├── 串口联调测试清单.md        — Hex 测试帧、联调步骤、故障排查速查表
├── 参考资料下载链接.md        — 各模块官方文档、工具下载地址
├── 第一性原理.md              — 第一性原理设计分析
└── 目录结构说明.md            — 本文件
```

---

## 5. `openmv-code/` — OpenMV 磁盘部署文件

```
openmv-code/
├── main.py           — 视觉追踪脚本（与 openmv_scripts/main_tracking.py 相同，用于直接拷入 OpenMV 磁盘）
├── trained.tflite    — FOMO TensorFlow Lite 模型文件（96×96 灰度输入）
└── labels.txt        — 模型标签文件（含 "drone" 类别）
```

> **部署方法**：将此目录下的三个文件拷贝到 OpenMV 内置存储根目录即可。
> `main.py` 与 `Drone_Tracking_RA6M5/openmv_scripts/main_tracking.py` 保持同步，修改算法请编辑 `openmv_scripts/main_tracking.py` 后同步到这里。

---

## 6. 参考资料

原 `References/` 目录已删除以精简仓库，所有官方文档、工具的下载地址已整理至 → [参考资料下载链接.md](参考资料下载链接.md)

---

## 生成文件与缓存（不需要关注）

| 目录/文件 | 来源 | 说明 |
|----------|------|------|
| `Drone_Tracking_RA6M5/Debug/` | 编译器 | 可通过 Clean Build 再生 |
| `Drone_Tracking_RA6M5/ra_gen/` | FSP Generate | 在 configuration.xml 改完后重新生成 |
| `Drone_Tracking_RA6M5/ra/` | FSP 框架 | 一般不需要修改 |
| `Drone_Tracking_WebCloud/__pycache__/` | Python | 自动生成 |
| `Drone_Tracking_WebCloud/.venv/` | pip | 虚拟环境 |
| `Drone_Tracking_WebCloud/drone_tracking.db` | SQLite | 运行时数据，首次启动自动创建 |
| `.git/` | Git | 版本控制 |
